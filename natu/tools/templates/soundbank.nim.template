#? stdtmpl(emit="f.write") | standard
## Generated by natu

let soundbankBin* = static staticRead("./soundbank.bin").MmSoundbankPtr
  ### Pointer to your game's soundbank data.

type
  Sample* {.size: 4.} = ${if sfxList.len > 0: "enum" else: "distinct uint32  # empty"}
    #for s in sfxList:
    $s
    #end for
  Module* {.size: 4.} = ${if modList.len > 0: "enum" else: "distinct uint32  # empty"}
    #for m in modList:
    $m
    #end for
